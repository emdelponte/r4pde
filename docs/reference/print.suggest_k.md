# Print method for suggest_k

Print method for suggest_k

## Usage

``` r
# S3 method for class 'suggest_k'
print(x, ...)
```

## Arguments

- x:

  An object of class `"suggest_k"`.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns `x`.
